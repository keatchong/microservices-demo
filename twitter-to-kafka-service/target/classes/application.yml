spring:
  application:
    name: twitter-to-kafka-service
  profiles:
    active: dev
  config:
    import: optional:configserver:http://localhost:8888
    username: spring_cloud_ser
    password: springCloud_Pwd!  
